{
  "name": "nest-auth",
  "private": true,
  "packageManager": "bun@1.3.14",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "check": "turbo run lint build"
  },
  "devDependencies": {
    "turbo": "^2.10.12"
  }
}
